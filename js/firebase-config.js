/* Fill this in with YOUR OWN Firebase project's config.
   Firebase console → Project settings (gear icon) → General →
   "Your apps" → Web app (</>) → SDK setup and configuration.
   These values are safe to expose in client-side code — they identify
   your project, they don't grant access on their own. Access control
   is handled by Firestore security rules (see README.md). */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
